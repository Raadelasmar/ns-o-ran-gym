# Python dependencies for the MLB training pipeline.
# Regenerated from the working gym-venv (Python 3.12.3).
#
# Install torch FIRST, from the CPU index, then this file:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#   pip install -r requirements.txt
# The CPU index matters: the default one pulls ~3.4 GB of CUDA libraries that
# this project never uses (the policy is a 64x64 MLP and the bottleneck is the
# simulator). The nvidia-* and triton packages are deliberately not pinned here.
#
# The ns-o-ran-gym package itself is NOT listed. Install it from your local
# clone as an editable install:
#   pip install -e ~/oran-project/ns-o-ran-gym
click==8.4.2
cloudpickle==3.1.2
contourpy==1.3.3
cycler==0.12.1
dill==0.4.1
drmaa==0.7.9
Farama-Notifications==0.0.6
filelock==3.32.3
fonttools==4.63.0
fsspec==2026.7.0
gitdb==4.0.12
GitPython==3.1.50
gymnasium==1.3.0
Jinja2==3.1.6
kiwisolver==1.5.0
MarkupSafe==3.0.3
matplotlib==3.11.0
mpmath==1.3.0
multiprocess==0.70.19
networkx==3.6.1
numpy==2.5.0
packaging==26.2
pandas==3.0.3
pillow==12.2.0
posix_ipc==1.3.2
pyparsing==3.3.2
python-dateutil==2.9.0.post0
pyzmq==27.1.0
SALib==1.5.2
scipy==1.18.0
sem==0.3.9.1
setuptools==84.0.0
six==1.17.0
smmap==5.0.3
stable_baselines3==2.9.0
sympy==1.14.0
tinydb==4.8.2
torch==2.13.0+cpu
tqdm==4.68.3
typing==3.7.4.3
typing_extensions==4.15.0
xarray==2026.4.0
